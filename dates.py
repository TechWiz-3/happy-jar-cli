# Converts the date after the --on switch to a strftime date and gets the
# output

class Dates():

    @classmethod
    def get_date_items(date):

        if date.lower() == "today":
            # get dt

            # strftime

            # search file

            # get string
